import { StyleSheet } from 'react-native';

export const s = StyleSheet.create({
    container: {

        // backgroundColor: 'red',
        alignItems: 'center',
        justifyContent: 'center',
    },
    txt: {
        fontSize: 28,
    }
});